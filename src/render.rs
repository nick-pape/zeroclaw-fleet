//! Renders a per-claw `config.toml` from `base.toml` + the claw's overlay,
//! with the orchestrator-injected fields (paired_tokens, MCP bearer placeholder,
//! trust_forwarded_headers, web_dist_dir) and the expanded `auto_approve` list.
